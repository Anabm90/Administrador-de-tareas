<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario

            h1 Inicia sesión en tu agenda virtual
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                
            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    //- label(for="email")
                    input(type="email" name="email" placeholder=" Introduce tu email" value= email)
                .campo
                    //- label(for="password")
                    input(type="password" name="password" placeholder=" Introduce tu  contraseña" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar sesión")
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aún no tienes cuenta?
                    a(href="/restablecer") ¿Olvidaste tu contraseña?

        script(src="/dist/bundle.js")