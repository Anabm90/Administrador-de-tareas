<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Confirma tu cuenta
        p Hola, para confirmar tu cuenta haz click en el siguiente enlace haz click en el siguiente enlace para restablecerlo.
        a(href=`${confirmarUrl}`) ACTIVAR MI CUENTA